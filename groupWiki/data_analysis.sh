/*First we download the sourcecode of the webpage, try to find some special patterns: for example a 'name' has its own class in html, we will try to get the infomation we want by searching these kind of patterns(ex:label).
we will use 'grep' instruction to search for the key words，together with the "awk" instruction to get the result.
Then we will use "awk" to get the information of the hyperlinks*/
